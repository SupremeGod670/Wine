<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Wine" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/vinho_claro</item>
    </style>

    <style name="BACKGROUND">
        <item name="android:background">@color/vinho_claro</item>
    </style>

    <style name="BACKGROUND_MENU">
        <item name="android:itemBackground">@color/vinho</item>
        <item name="android:textColorHint">@color/white</item>
    </style>

    <style name="EDITTEXT">
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textColorHint">@color/black</item>
    </style>

    <style name="Theme.Wine" parent="Base.Theme.Wine" />
</resources>